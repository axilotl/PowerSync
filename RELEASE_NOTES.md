<!-- release: v2.12.478 -->

## What's Changed

**Fix SolarEdge setup crash**
PowerSync no longer crashes when opening the initial SolarEdge battery setup form. The SolarEdge setup page now uses local setup defaults instead of the options-flow helper that only exists after an integration entry has already been created, so new SolarEdge systems can complete onboarding normally.

Update available via HACS
