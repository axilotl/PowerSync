<!-- release: v2.12.283 -->

## What's Changed

**Restore the previous dashboard behavior**
This release rolls back the extra dashboard strategy and energy-flow rendering changes from v2.12.282. The missing home scene was caused by a stale Chrome instance rather than a PowerSync dashboard regression, so the auto-created dashboard is back to the leaner, previously released behavior.

Update available via HACS
