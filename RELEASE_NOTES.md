<!-- release: v2.12.307 -->

## What's Changed

**More visible dashboard refresh**
The PowerSync dashboard chart cards now have a clearer visual hierarchy with stronger Home Assistant themed card styling, accent bars, subtle chart color washes, larger plot areas, live value pills, and endpoint markers. Tablet layouts now give the main chart/flow column more priority so the refreshed dashboard is visibly different rather than only internally refactored.

Update available via HACS
