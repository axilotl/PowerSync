<!-- release: v2.12.697 -->

## What's Changed

**Flow Power dashboard price alignment**
Flow Power's standard Home Assistant current import/export price sensors now prefer the same canonical tariff schedule used by the optimizer, Sigenergy tariff sync, and the PowerSync tariff-price endpoint. This keeps the default Electricity Price dashboard chart aligned with Flow Power Live Data after the account-aware PEA calculation is applied.

Update available via HACS
