<!-- release: v2.12.702 -->

## What's Changed

**Flow Power display prices during force sessions**
PowerSync now keeps the stored tariff schedule fresh while a force charge or force discharge session is active, without overwriting the battery/cloud force tariff. This keeps the standard current import/export price sensors aligned with the latest Flow Power tariff schedule instead of showing stale prices until the force session ends.

Update available via HACS
