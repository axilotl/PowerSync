## What's Changed

**Setup: Battery selector now uses a dropdown**
The initial battery-system step now renders as a dropdown instead of a long inline list. This keeps later-added systems such as Solax visible in Home Assistant's config-flow dialog instead of being pushed below the fold on smaller screens.

Update available via HACS
