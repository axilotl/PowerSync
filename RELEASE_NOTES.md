<!-- release: v2.12.324 -->

## What's Changed

**Neovolt fleet dashboard data fix**
Fixes the NeoVolta fleet coordinator startup path so the combined PowerSync energy sensors are populated correctly when multiple Neovolt inverter entries are configured. This ensures dashboards and charts can read the single aggregated solar, grid, battery, home load, and SOC values introduced in v2.12.323.

Update available via HACS
