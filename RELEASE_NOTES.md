<!-- release: v2.12.399 -->

## What's Changed

**Smart Optimization setup text now matches options**
The initial setup flow and post-setup options flow now use the same Smart Optimization page title, description, shared field labels, and helper text, so the Profit Max target time and target SOC controls render consistently wherever users configure them.

Update available via HACS
