<!-- release: v2.12.629 -->

## What's Changed

**Restore missing EV dashboard card**
PowerSync now self-heals older or stale dashboard layout configs that loaded the PowerSync dashboard without the EV card. If the layout config is missing the EV panel, the dashboard appends it automatically so the EV status API can load and the card can appear after updating.

Update available via HACS
