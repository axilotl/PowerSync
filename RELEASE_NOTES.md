<!-- release: v2.12.639 -->

## What's Changed

**Dashboard chart tooltips stay above graph lines**
Generic PowerSync dashboard chart tooltips now render in their own top stacking layer above the chart SVG paths. This extends the tooltip layering fix from the optimizer SOC/price charts to the other dashboard graphs, so forecast and history chart lines no longer cut through hover text.

Update available via HACS
