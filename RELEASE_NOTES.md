<!-- release: v2.12.579 -->

## What's Changed

- Fixed a Profit Max export guard bug where the forecast home-load reserve floor could stop a scheduled export even when Auto-apply optimizer reserve was disabled.
- Auto-apply optimizer reserve now uses the forecast export bridge floor only when that setting is enabled.
- Updated public support links and integration metadata so issue reports go to Discord instead of GitHub Issues.

Update available via HACS.
