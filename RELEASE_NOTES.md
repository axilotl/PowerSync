## What's Changed

**Devices: All Entities Back Under One Device**
The sub-device grouping introduced recently (PowerSync Battery, PowerSync LP Optimizer, PowerSync Flow Power, etc.) created 8 separate devices that cluttered the integration page and showed up individually in Areas. All entities are now back under a single parent device, keeping the integration page clean. Entity IDs and history are unchanged.

Update available via HACS
