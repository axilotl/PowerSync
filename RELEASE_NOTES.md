## What's Changed

**HA dashboard scene text realigned to the scene layout tool**
The previous hotfix made the overlay wording visible again, but the text and guide positions still did not line up with the authored scene-layout-tool output. The HA dashboard now uses the tool's absolute SVG coordinates directly for overlay text and guides, so labels, values, and indicators match the designed scene layout again.

Update available via HACS
